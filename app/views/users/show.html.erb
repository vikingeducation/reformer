<h1>Info for <%= @user.username %></h1>

<%= link_to 'Edit', edit_user_path(@user) %>

<table>
  <thead>
    <tr>
      <th>username</th>
      <th>email</th>
      <th>password</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @user.username %></td>
      <td><%= @user.email %></td>
      <td><%= @user.password %></td>

    </tr>
  </tbody>
</table>
