<%= content_tag :h1, "Hello World!" %>

<%= render partial: 'shared/user_form', locals: { action: users_path, method: 'post', user: @user } %>

<%= render partial: 'shared/form_tag_user_form', locals: { action: users_path, method: 'post', user: @user } %>